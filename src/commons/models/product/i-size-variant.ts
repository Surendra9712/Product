export interface ISizeVariant{
    variantName: string,
    price: number,
    strikePrice: number,
    offPercent: number,
    minOrder: number,
    maxOrder: number,
    status: boolean,
    images: string[],
    productCode: string,
    _id: string
}